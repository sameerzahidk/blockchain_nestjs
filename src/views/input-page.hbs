<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Input Page</title>
</head>
<body>
    <h1>Input Page</h1>
    <form action="/input-page/submit" method="POST">
        <label for="ethAmount">Enter ETH amount:</label>
        <input type="number" id="ethAmount" name="ethAmount" step="0.01" required value="{{ethAmount}}">
        <button type="submit">Submit</button>
    </form>

    {{#if message}}
        <h2>{{message}}</h2>
        <p>Conversion rate of {{ethAmount}} ETH to BTC is {{btcWithFee}} inclusive conversion charges (0.03USD).</p>
    {{/if}}
</body>
</html>